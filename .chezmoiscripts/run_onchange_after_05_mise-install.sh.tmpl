#!/bin/bash

set -eufo pipefail

# mise config: {{ output "sha256sum" (joinPath .chezmoi.homeDir ".config/mise/config.toml") | trim }}

echo ":: [05] Installing mise tools"

if command -v mise &>/dev/null; then
    mise install --yes
else
    echo "    Skipped (mise not found)"
fi
